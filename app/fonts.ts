// Electron loads the exported Next.js app from file://, which needs relative
// asset paths. next/font requires an absolute assetPrefix, so font variables
// are defined in CSS instead of generated during the Next build.
export const fontVariables = ''
